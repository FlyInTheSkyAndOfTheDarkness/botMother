# План задач: AI Agents Platform

## ✅ Что уже сделано и работает:

1. **Убрали Flow Builder** - полностью удален
2. **Исправили конфликт Vue delimiters** - `{{ }}` → `[[ ]]`
3. **Telegram бот** - создан, интегрирован, запускается/останавливается
4. **WhatsApp agent handler** - создан, обрабатывает сообщения
5. **Live Chat UI и API** - созданы, отображают данные
6. **SerpAPI** - интегрирован для интернет-поиска
7. **Сохранение ключей** - исправлено (не перезаписываются пустыми)
8. **База данных агентов** - создана, миграции работают
9. **Favicon и иконка** - добавлены

---

## ⚠️ Что сделано, но НЕ РАБОТАЕТ:

### 1. **WhatsApp не отвечает на сообщения**
   - **Проблема:** Agent handler может не находить правильную интеграцию или агент не активен
   - **Нужно проверить:**
     - Логика поиска интеграции по device JID
     - Проверка `IsActive` и `IsConnected`
     - Логирование для отладки

### 2. **Telegram не отвечает на сообщения**
   - **Проблема:** Бот может не обрабатывать сообщения или агент не активен
   - **Нужно проверить:**
     - Логика `HandleIncomingMessage` в `agentService`
     - Проверка `IsActive` агента
     - Логирование для отладки

### 3. **Live Chat SendMessage для WhatsApp - не реализовано**
   - **Проблема:** Есть TODO в `conversation.go:228`
   - **Нужно:** Реализовать отправку сообщений через WhatsApp client

### 4. **Проверка IsActive и IsConnected**
   - **Проблема:** Может не проверяться правильно
   - **Нужно:** Добавить проверки и логирование

---

## ❌ Что не сделано:

1. **Instagram интеграция** - только UI, нет backend
2. **Dashboard** - пустой компонент
3. **Settings** - пустой компонент

---

## 📋 План исправлений (по приоритету):

### Приоритет 1: Критичные проблемы

1. ✅ **Добавить детальное логирование** для WhatsApp и Telegram
2. ✅ **Проверить и исправить логику поиска интеграций** в WhatsApp handler
3. ✅ **Проверить и исправить логику обработки сообщений** в Telegram
4. ✅ **Реализовать SendMessage для WhatsApp** в Live Chat

### Приоритет 2: Улучшения

5. ✅ **Проверить работу SerpAPI** (интернет-поиск)
6. ✅ **Добавить проверки IsActive/IsConnected** с логированием

---

## 🔍 Шаги отладки:

1. Добавить логи в ключевых местах:
   - WhatsApp: `agent_handler.go` - когда находит/не находит интеграцию
   - Telegram: `bot.go` - когда получает сообщение и обрабатывает
   - AgentService: `agent.go` - когда проверяет IsActive

2. Проверить базу данных:
   - Агенты активны (`is_active = 1`)?
   - Интеграции подключены (`is_connected = 1`)?
   - Device ID совпадает?

3. Проверить инициализацию:
   - Agent handler инициализирован?
   - Telegram bot manager инициализирован?
   - Agent service инициализирован?

